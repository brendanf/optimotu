% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/optimize_thresholds.R
\name{optimize_thresholds}
\alias{optimize_thresholds}
\title{Optimize clustering thresholds for taxonomically identified reference sequences}
\usage{
optimize_thresholds(
  taxonomy,
  refseq,
  ranks = c("kingdom", "phylum", "class", "order", "family", "genus", "species"),
  dist_config = dist_wfa2(),
  threshold_config = threshold_uniform(0, 0.4, 0.001),
  clust_config = clust_tree(),
  parallel_config = parallel_concurrent(threads = 1),
  min_taxa = 5L,
  min_refseq = 2L * min_taxa,
  id_col = "seq_id",
  measures = c("MCC", "RI", "ARI", "FMI", "MI", "AMI", "FM"),
  verbose = FALSE,
  seq_idx = NULL,
  seq_file = NULL,
  clustering_memory_budget_mb = NULL,
  retry_on_memory_exhaustion = TRUE,
  retry_split_strategy = c("overlap", "balanced"),
  retry_reduce_threads = TRUE,
  retry_min_threads = 1L,
  retry_overpartition = TRUE,
  retry_subproblem_factor = 2L
)
}
\arguments{
\item{taxonomy}{(\code{data.frame}) taxonomic identifications of the reference
sequences.  Must include a column for each rank in \code{ranks} and a column for
sequence identifiers, defined by \code{id_col}. Any additional columns are ignored.}

\item{refseq}{(named \code{character}, file name,
\code{\link[Biostrings:XStringSet-class]{DNAStringSet}}, \code{data.frame},
\code{fastqindexr_index}, or character vector of \code{.fqi} paths) the
reference sequences}

\item{ranks}{(\code{character} vector) the taxonomic ranks in \code{taxonomy}}

\item{dist_config}{(\verb{op timotu_dist_config}) specification of the pairwise
distance algorithm to use, as created by \code{dist_config()} or its helpers}

\item{threshold_config}{(\code{optimotu_threshold_config}) specification of the
thresholds to test, as created by \code{threshold_config()} or its helpers}

\item{clust_config}{(\code{optimotu_clust_config}) specification of the clustering
algorithm to use, as created by \code{clust_config()} or its helpers}

\item{parallel_config}{(\code{optimotu_parallel_config}) specification of the
parallelization scheme to use, as created by \code{parallel_config()} or its
helpers}

\item{min_taxa}{(\code{integer(1)}) the minimum number of subtaxa at a given rank
which must belong to a taxon to optimize thresholds for that rank within that
taxon. Must be at least 2.}

\item{min_refseq}{(\code{integer(1)}) the minimum number of reference sequences
which must belong to a taxon to optimize thresholds for that taxon. Must be
at least \code{min_taxa} (but should probably be more).}

\item{id_col}{(\code{character(1)}) the name of the column in \code{taxonomy} which
contains sequence identifiers}

\item{measures}{(\code{character}) one or more measures to calculate optimum
thresholds for}

\item{verbose}{(\code{logical(1)} or \code{integer(1)}) whether to print progress
messages; values greater than 1 (or TRUE) print more}

\item{seq_idx}{optional 1-based subset indices for \code{refseq}; see \code{\link[=seq_as_char]{seq_as_char()}}.}

\item{seq_file}{optional path overrides for index-backed \code{refseq} only; see
\code{\link[=seq_as_char]{seq_as_char()}}. Taxonomy rows must match the selected sequence IDs.}

\item{clustering_memory_budget_mb}{(\code{numeric(1)} or \code{NULL}) Best-effort
memory budget for clustering-owned native structures, in MB. This is not a
hard process-memory cap; set below machine limits for safer behavior.}

\item{retry_on_memory_exhaustion}{(\code{logical(1)}) Retry clustering batches if
the clustering memory budget is exceeded.}

\item{retry_split_strategy}{(\code{character(1)}) Strategy used to split failed
batches. \code{"overlap"} prefers keeping related subsets together and
\code{"balanced"} splits by size.}

\item{retry_reduce_threads}{(\code{logical(1)}) Whether to reduce thread count
before splitting on budget failure.}

\item{retry_min_threads}{(\code{integer(1)}) Lower bound for thread-reduction
retries.}

\item{retry_overpartition}{(\code{logical(1)}) Whether to retry with more pair
subproblems than threads to reduce per-subproblem mapped state.}

\item{retry_subproblem_factor}{(\code{integer(1)}) Multiplicative growth factor
for subproblem count during overpartition retries.}
}
\value{
(\code{data.frame}) a data frame with the following columns:
\itemize{
\item \code{rank} (\code{character}) the rank being optimized
\item \code{superrank} (\code{character}) the containing rank for optimization
\item \code{supertaxon} (\code{character}) the containing taxon
\item \code{measure} (\code{character}) the clustering quality measure
\item \code{threshold} (\code{numeric}) the threshold which maximizes the measure
\item \code{value} (\code{numeric}) the value of the measure at the threshold
}
}
\description{
Optimize clustering thresholds for taxonomically identified reference sequences
}
